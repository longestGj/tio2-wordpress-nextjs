# Semrush Evidence

Semrush observations were collected through the authenticated Codex in-app browser on 2026-08-29. The normalized values used for decisions are preserved in `../../keyword-evidence.json` because Semrush results are dynamic and the live bulk-analysis URL is session-dependent.

`semrush-us-bulk-2026-08-29.png` is the full-page snapshot of the 71-candidate US variant review. Thirty decision-related variant observations are normalized in the JSON; 71 is the reviewed candidate count, not the number of stored rows.

`semrush-us-exact-review-2026-08-29.png` records the 11-query remediation batch used for Solar Film and the other exact-query evidence gaps. Every normalized row has a unique `observationId`. If screenshots or exports are added during a refresh, use dated filenames and do not overwrite earlier evidence. A screenshot is supporting evidence only; the normalized observation record remains the audit source for individual values.
