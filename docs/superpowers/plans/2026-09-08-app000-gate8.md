# APP-000 Gate 8 implementation plan

1. Preserve the existing Site A `/applications` implementation and route Malaysia requests through a scope-specific APP-000 loader.
2. Add one checked-in approved APP-000 semantic contract, one private WordPress content identity and one GraphQL projection with explicit route-readiness booleans.
3. Validate exact scope, path, publish state, contract bytes and all 30 ordered Grade occurrences before creating the public DTO.
4. Render the approved breadcrumb and M1–M5 content with current Malaysia Header/Footer/Consent owners. Apply atomic destination omission and plain-text Grade fallback from readiness only.
5. Emit exact metadata plus CollectionPage/BreadcrumbList and a conditional child-application ItemList; keep indexing disabled in this environment and strip internal IDs from public output.
6. Add focused unit, integration, WordPress-contract, route, schema, conditional-state and browser tests before implementation claims.
7. Run targeted tests, changed-file lint, typecheck, production build and multi-viewport Chromium/Firefox checks. Record all 30 edge outcomes and shared-instance identities.
8. Commit implementation, then evidence/receipt/rollback record and a schema-valid Gate 8 manifest. Hold the exact local production candidate until Gate 9 responds.

No consumer-page edits, real external submission, main merge, push, deployment, publication, DNS or indexing action is in scope.
