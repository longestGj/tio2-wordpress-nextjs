# MARKET-UK-001 Implementation Plan

> Execute inline with superpowers:executing-plans and test-driven-development. Existing isolated worktree; no new project or worktree.

**Goal:** Implement only `/markets/united-kingdom/` for external Gate 9 inspection.
**Architecture:** Existing `tio2_market_page` CMS type, singleton scope-validated UK record and Next server-rendered UK body. Consume the unique shared Chrome without page CSS inheritance. Extend the existing RFQ allowlist, never duplicate the form.
**Tech stack:** WordPress/PHP, GraphQL codegen, Next 16, React, CSS modules, Vitest, Playwright/Axe.
**Spec:** D:/23MySec/pages/markets/06_handoff/MARKET-UK-001_GATE7_HANDOFF_PACKAGE_V0.1.md and its four companion handoff records; Gate 5 visual/source hashes locked.

## Constraints

- `site_scope=tio2-my`; exact UK Buyer Clean, 14-module assembly, 5 Application links and 6 Grade links.
- Shared RFQ `/request-a-quote/`; only two body RFQs include the approved UK/source query.
- 3 breadcrumb items, 2 anchors, current non-link. Native FAQ initial HTML.
- Immutable evergreen Trade default; no dated state or Trade routes. No Process/recommendation/comparison output.
- No other page, D23 edit, production CMS write, deployment, publishing, indexing, or Gate 9/10 approval.

## Execution checklist

- [x] Red: UK prefill and cache identity boundaries failed before initial implementation; review-correction PHP tests also failed before both fixes (see final verification record).
- [x] Data boundary: strict UK JSON/DTO/types/query, GraphQL/schema/codegen and PHP resolver; missing/multiple/invalid/cross-scope fail closed.
- [x] Shared integration: UK cache identity, webhook path and metadata mutation hooks, canonical slash and RFQ allowlist; EU/RFQ/query regression retained.
- [x] Body: exact approved copy, module/route order, 6 initial FAQ answers, RFQ/Breadcrumb, conditional Grade ItemList, noindex/sitemap exclusion verified.
- [x] Runtime: fixture-backed 1440/768/390, shared Chrome, a11y/focus/reflow, rendered SEO/schema, route ledger and intercept-only RFQ evidence captured. All six unavailable Application dependencies remain explicit blockers.
- [x] Local verification and evidence: focused Vitest 224, PHP, lint, typecheck and online-fixture build pass; Playwright 21 pass; tracked tsconfig restored. Final report and hash manifest prepared.
- Commit/preview delivery: preserve this branch/worktree; independent commit and post-commit ordinary localhost preview outcome are reported to the controller. No merge, push or production operation. See `docs/verification/market-uk-001/GATE8_FINAL_REPORT_2026-09-05.md` for release limitations.

## Check contracts

`resolveMalaysiaRfqPrefill({market: 'United Kingdom', source_page: 'MARKET-UK-001'})` must return only editable `destination_country='United Kingdom'` plus trusted `sourcePageId`. Bare RFQ has neither. UK query returns `Promise<MalaysiaUkMarketPageDto>` or throws, never an unreachable null path. JSON-LD contains WebPage/BreadcrumbList, adding the six named Grade ListItems only if all six scoped targets are validated. No release/index permission is enabled by this implementation.
