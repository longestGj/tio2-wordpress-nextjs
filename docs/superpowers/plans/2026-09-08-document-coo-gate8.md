# DOC-COO Gate 8 implementation plan

**Scope:** `tio2-my` / `DOC-COO` / `/documents/certificate-of-origin/` on `codex/poland-development`.

**Authority:** dispatch `G8-BR-CL-COO-FOUR-20260908-01`, Gate 6 package `COO-G6-HANDOFF-04`, Buyer Clean V0.2, behavior contract V0.1, and visual freeze `COO-G5-V01-SOURCE-01`.

**Starting identity:** repository and worktree `D:\16Wordpress_nextjs`; branch `codex/poland-development`; pre-work HEAD `c2764e6138aa0dc37d1f6cab04bc1ae39f45aecb`; dirty working tree containing authorized earlier Poland/Brazil/PRODUCT-PROC-CL work plus process documentation. DOC-COO files do not exist at start. Preserve every pre-existing path and record the final inventory in the receipt.

## Tasks

- [x] Add the complete `DOC-COO-G9-FOCUSED` tests before implementation and capture an intentional RED result against the unchanged runtime.
- [x] Add a hash-locked, repository-relative Buyer Clean content fixture and a strict `tio2-my` WordPress singleton contract with wrong-scope, wrong-route, duplicate and mismatched-payload failure modes.
- [x] Add the GraphQL singleton, generated query, strict TypeScript DTO and page-specific cache identity; keep governance state outside the public component boundary.
- [x] Extend CONV-DOC normalization for the one approved document type and hidden `DOC-COO` attribution; make supported URL state deterministic on initial load, Back, Forward and direct revisit.
- [x] Implement the six exact sections, semantic table-to-record responsive behavior, shared Malaysia Chrome, approved actions, metadata, noindex/nofollow and the two-node WebPage/BreadcrumbList graph.
- [x] Wire the exact route, webhook and signed revalidation path without adding sitemap publication.
- [x] Seed and validate a real local WordPress record, then prove real GraphQL readback and fail-closed behavior.
- [x] Run the unchanged focused tests to GREEN, typecheck, relevant lint, Malaysia optimized build, signed revalidation, nine-width geometry/Axe checks and full-page visual inspection at 1440/768/390.
- [x] Run directly affected Malaysia shared regressions, create the Gate 8 development receipt, and record limits: no live form send, no fresh external-source release review, no deployment/indexing authorization.
