# tio2-my repeated CMS installation repair — local execution

User instruction: modify the local installer, finish local release-flow tests, then release to production. Business content stays at main `eebfb0299ab8d3a4de7241211be9b6b20b9ad090`; the development database is excluded. Repair branch is `codex/fix-cms-repeat-upgrade`. Full-flow testing also required two frontend publication fixes, recorded below; the eventual frontend needs new candidate evidence. No production write has been performed during this repair.

## Implemented behavior

The administrator CLI accepts a registered completed installation via `--upgrade-from`. It validates the prior completion, sealed importer, actual plugin bytes, current frontend and current production receipt. Exact existing maintenance rules are reusable; foreign or modified rules remain rejected. The old importer is journalled, stopped and retained by immutable ID before replacement. Failure restores the original importer ID, plugin and PHP files. Recovery reconstructs upgrade mode from its saved plan even when the flag is omitted. New-importer verification checks the durable creation ID; legacy completed installations without that newer journal are accepted only against their completed receipt.

Known installation errors now report action/stage and the non-secret ReleaseError reason. Unexpected errors retain the generic response.

The full-content check exposed an existing metadata-contract gap: several current pages use approved frontend publication metadata rather than legacy CMS field names. Optional root-enrolled `publishedSeo` binds those expectations to the unchanged original content SEO hash. A later SEO change cannot pass against that old binding. The map generator reads exact Git inputs and requires all 57 published records. Legal pages use their dynamic CMS metadata and verify body fragments, links' visible text, table cells and action text. Restricted About evidence is rejected for separate review. The content package remains unchanged.

## Recorded local evidence so far

- Related repeated-installation/resource/backend tests: 58 passed after immutable-ID fix. Expanded regression: 204 run, 201 passed and 3 POSIX-only skips. Those three were subsequently run in a root-owned Linux container: all passed.
- Related installer/hooks/map/finalization regression after metadata additions: 77 passed. Independent final hooks/map review: 22 passed, no remaining code blocker reported. These test sets overlap and must not be added together as a unique count.
- Real WordPress/MariaDB resource rehearsal at exact `97b067e3db24348d2f6e8012700e893061469705`: PASS; successive upgrade, exact importer restoration, replacement-create failure recovery, private read-only execution tree, other scope preserved, HTTP restored. Ignored receipt: `D:/16Wordpress_nextjs/.production/runs/20260914-main-eebfb029-preflight/cms-repeat-resources-97b067e3.json`.
- Full isolated backend orchestration: first failure/rollback, first success, upgrade failure/rollback and upgrade success all passed; maintenance reopened, DB writes restored and both seeded scopes preserved. The repeat against exact committed program and CMS artifact bytes passed at `a011675becabaa88271c75e3ff0fa5a52f9b2b9b`, recording per-file program hashes in `cms-repeat-backend-a011675b.json`. Its frontend is a generated HTTP fixture, not real Next business acceptance.
- Fixed frontend controller/Docker/age/Nginx rehearsal: 11 cases passed, including both new candidate generations and previous-generation rollback. Evidence: `.tmp/frontend-release/d16-front-4d4b190ede6b4d2c864393af0c3ce38b/evidence.json`. The generated frontend and synthetic mail/completion documents test mechanics only. Cleanup verified; real production mail was not sent.
- Full main package is 57 records, content SHA-256 `bacd347767172d22c38ef92665ca484c43a8c5f6c758c436974b30a80284caa3`. Real WordPress/WPGraphQL, Next production build, Nginx and installed ContentHooks passed at `caff97fe91e35e9a45f5e1bfe9560269f3b42352`, Build `M2_1imRWltKG8bTZJ9_Le`. All registered HTTP/SEO/sitemap/body probes passed after import and signed refresh. A second transaction changed isolated HOME text, reached actual page verification, injected failure, restored the full DB and reverified all 57 records without restarting Next. The final content hash matches the original package. Unregistered product path remained 404. Receipt: `main-content-real-caff97fe.json` in the same ignored run directory. Owned resources were cleaned and the process exited 0. The restored About screenshot was visually inspected; this is a representative screenshot, not a full visual acceptance suite.
- Frontend corrections: integrate develop's registered Malaysia product refresh paths and dependency tags; compare Grade JSON objects without depending on key order; allow regeneration of Grade cache entries while preserving explicit scope/slug/public-route guards. Runtime source changes versus original main are limited to the refresh API, Grade DTO and Grade route. CMS/business input files are unchanged. Earlier failed runs remain failures, including the confirmed `NoFallbackError` after cache expiration.
- Current-source regression, excluding historical `.tmp` snapshots: 45 files / 715 tests passed; post-merge targeted regression 4 files / 116 passed; Grade route/scope gating 2 files / 27 passed. These sets overlap. Earlier larger Vitest counts included two old snapshot copies and are not unique-source totals. Independent reviews found no remaining code blocker after the stale gating assertion was updated and Site B family rejection checked.

## Remaining gates

Integrate the tested candidate into actual develop/main and run the new identity-bound main prerelease. Earlier eeb live-mail evidence retains its original identity and cannot be relabelled for the new frontend candidate. The existing installation verification package contains only HOME, whose content and SEO match the main package; its compatibility probe can remain in a larger 57-page registration during upgrade/finalization. The finalization approval hash remains unchanged. Production administrator/CMS changes, frontend/content publication and production final acceptance still need their actual receipts. No claim of `PRODUCTION_VERIFIED` is made here.
