// Next discovers robots metadata only at the app root. Keep the existing
// site/environment policy and its tested helper in the shared implementation.
import robots from './(en)/robots'

export default robots
