# Historical legal frontend delivery: machine-readable projection

This record fills four frontend paths missing from the existing machine-readable coverage of production 27f0a0da to the current candidate. It does not change the historical implementation, tests or mail evidence.

The committed [legal delivery receipt](../cms-read-decoupling-legal.md), as recorded at 05a52794, states the actual develop fast-forward from 7c849af2 to 47175c13fcc06c61993bd6a3d4097c12e757f5de. All four listed frontend paths belong to that delivery and their current contents equal that commit. Independent review confirmed the ancestry and file bytes.

The companion JSON describes only those four frontend paths. Its empty content/host scopes and `cmsContractChanged=false` classify this frontend subset; they do not deny that the original legal delivery also changed the CMS plugin. That plugin has its own authorized administrator installation and must match the installed CMS baseline before frontend packaging can proceed. The remaining frontend differences are covered by the existing 27f0a0da-to-52cb1925 receipt and the current content-publication repair receipt.

This is a retrospective representation of an evidenced develop merge, not a new approval, test pass, production installation or deployment. The main candidate must be frozen again after including it.
